import { Typography } from "@mui/material";

const ContactMe = () => {
  return (
    <div className="flex flex-col">
      <Typography variant="caption">Want to Contact Me</Typography>
      <Typography variant="subtitle2">Say Hello 🤝</Typography>
      <Typography variant="subtitle1">Send me a Message!</Typography>
    </div>
  );
};

export default ContactMe;
